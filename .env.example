# 目的：列出运行与交易所需的环境变量，不包含真实密钥
# 方法：复制为 .env 后填入真实值，.env 不提交到 Git

# CLOB 认证（必填）
PRIVATE_KEY=           # 钱包私钥，从 Polymarket 导出或 Web3 钱包
FUNDER_ADDRESS=        # Polymarket 代理钱包地址（polymarket.com/settings 查看）

# L2 API 凭证（可选，不填则用 L1 自动创建/派生）
# API_KEY=
# API_SECRET=
# API_PASSPHRASE=

# 运行模式
PAPER_TRADING=true     # true=纸面/模拟不下单，false=实盘
# LIVE_TRADING=false

# 可选：配置文件路径（默认 config/config.yaml）
# CONFIG_PATH=config/config.yaml

# Telegram 推送（套利机会提醒，可选；详见 docs/TELEGRAM_SETUP.md）
# TELEGRAM_BOT_TOKEN=   # 从 @BotFather 创建 Bot 后获得的 token
# TELEGRAM_CHAT_ID=     # 私聊：你的用户 id（正数）；Channel：-100 开头的频道 id（负数）
